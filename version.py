"""Single source of truth for the EleViewer application version.

Every module that reports or compares the app version (updater, feedback,
what's-new, crash reporter, window title) must import APP_VERSION from here.
"""

APP_VERSION = "1.3.3"
