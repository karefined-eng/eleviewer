{
  "name": "eleviewer",
  "version": "1.0.0",
  "description": "Open anything. Edit everything. Nothing to install.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm run build --prefix site",
    "dev": "npm run dev --prefix site"
  },
  "private": true
}
